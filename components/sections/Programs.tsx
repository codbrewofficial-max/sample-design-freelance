import React from 'react';
import { SectionProps } from '../SectionRenderer';

export default function Programs({ id, data, content }: SectionProps) {
  console.warn(`Section Programs (${id}) belum diimplementasikan penuh, ini placeholder`);
  return null;
}
