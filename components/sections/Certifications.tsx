import React from 'react';
import { SectionProps } from '../SectionRenderer';

export default function Certifications({ id, data, content }: SectionProps) {
  console.warn(`Section Certifications (${id}) belum diimplementasikan penuh, ini placeholder`);
  return null;
}
