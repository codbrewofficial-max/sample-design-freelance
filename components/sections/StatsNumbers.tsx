import React from 'react';
import { SectionProps } from '../SectionRenderer';

export default function StatsNumbers({ id, data, content }: SectionProps) {
  console.warn(`Section StatsNumbers (${id}) belum diimplementasikan penuh, ini placeholder`);
  return null;
}
