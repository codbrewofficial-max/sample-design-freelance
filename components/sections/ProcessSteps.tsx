import React from 'react';
import { SectionProps } from '../SectionRenderer';

export default function ProcessSteps({ id, data, content }: SectionProps) {
  console.warn(`Section ProcessSteps (${id}) belum diimplementasikan penuh, ini placeholder`);
  return null;
}
