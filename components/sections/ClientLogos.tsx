import React from 'react';
import { SectionProps } from '../SectionRenderer';

export default function ClientLogos({ id, data, content }: SectionProps) {
  console.warn(`Section ClientLogos (${id}) belum diimplementasikan penuh, ini placeholder`);
  return null;
}
