import React from 'react';
import { SectionProps } from '../SectionRenderer';

export default function Facilities({ id, data, content }: SectionProps) {
  console.warn(`Section Facilities (${id}) belum diimplementasikan penuh, ini placeholder`);
  return null;
}
