import React from 'react';
import { SectionProps } from '../SectionRenderer';

export default function ProductServiceGrid({ id, data, content }: SectionProps) {
  console.warn(`Section ProductServiceGrid (${id}) belum diimplementasikan penuh, ini placeholder`);
  return null;
}
