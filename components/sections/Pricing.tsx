import React from 'react';
import { SectionProps } from '../SectionRenderer';

export default function Pricing({ id, data, content }: SectionProps) {
  console.warn(`Section Pricing (${id}) belum diimplementasikan penuh, ini placeholder`);
  return null;
}
