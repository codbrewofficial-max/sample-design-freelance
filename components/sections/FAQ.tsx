import React from 'react';
import { SectionProps } from '../SectionRenderer';

export default function FAQ({ id, data, content }: SectionProps) {
  console.warn(`Section FAQ (${id}) belum diimplementasikan penuh, ini placeholder`);
  return null;
}
